import React, { FC } from 'react'
import { Layout } from 'antd'

const { Footer } = Layout

const AppFooter: FC = () => {
	return (
		<Footer className='text-center text-white bg-skyblue-1'>Footer</Footer>
	)
}

export default AppFooter